{
  "/tokens": {
    "implementations": {
      "POST": {
        "generateToken": {
          "notes": "Create a new JWT instance",
          "require": {"body": ["user_email", "user_password"]}
        }
      }
    }
  }
}
